[build]
cmd = "npm install --include=dev && npx prisma generate && npm run build"

[deploy]
startCommand = "npx prisma db push --accept-data-loss && npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
